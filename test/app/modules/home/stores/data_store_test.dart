import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:music_player/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // DataStore data;
  //
  setUp(() {
    //     data = HomeModule.to.get<DataStore>();
  });

  group('DataStore Test', () {
    //   test("First Test", () {
    //     expect(data, isInstanceOf<DataStore>());
    //   });

    //   test("Set Value", () {
    //     expect(data.value, equals(0));
    //     data.increment();
    //     expect(data.value, equals(1));
    //   });
  });
}
