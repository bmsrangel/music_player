import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:music_player/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // PlaylistController playlist;
  //
  setUp(() {
    //     playlist = HomeModule.to.get<PlaylistController>();
  });

  group('PlaylistController Test', () {
    //   test("First Test", () {
    //     expect(playlist, isInstanceOf<PlaylistController>());
    //   });

    //   test("Set Value", () {
    //     expect(playlist.value, equals(0));
    //     playlist.increment();
    //     expect(playlist.value, equals(1));
    //   });
  });
}
