import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PlaylistPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PlaylistPage(title: 'Playlist')));
    //  final titleFinder = find.text('Playlist');
    //  expect(titleFinder, findsOneWidget);
  });
}
