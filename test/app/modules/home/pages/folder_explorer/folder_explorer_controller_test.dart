import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:music_player/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // FolderExplorerController folderexplorer;
  //
  setUp(() {
    //     folderexplorer = HomeModule.to.get<FolderExplorerController>();
  });

  group('FolderExplorerController Test', () {
    //   test("First Test", () {
    //     expect(folderexplorer, isInstanceOf<FolderExplorerController>());
    //   });

    //   test("Set Value", () {
    //     expect(folderexplorer.value, equals(0));
    //     folderexplorer.increment();
    //     expect(folderexplorer.value, equals(1));
    //   });
  });
}
