import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FolderExplorerPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FolderExplorerPage(title: 'FolderExplorer')));
    //  final titleFinder = find.text('FolderExplorer');
    //  expect(titleFinder, findsOneWidget);
  });
}
