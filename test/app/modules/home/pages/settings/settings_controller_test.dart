import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:music_player/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // SettingsController settings;
  //
  setUp(() {
    //     settings = HomeModule.to.get<SettingsController>();
  });

  group('SettingsController Test', () {
    //   test("First Test", () {
    //     expect(settings, isInstanceOf<SettingsController>());
    //   });

    //   test("Set Value", () {
    //     expect(settings.value, equals(0));
    //     settings.increment();
    //     expect(settings.value, equals(1));
    //   });
  });
}
