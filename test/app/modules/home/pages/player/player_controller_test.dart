import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:music_player/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // PlayerController player;
  //
  setUp(() {
    //     player = HomeModule.to.get<PlayerController>();
  });

  group('PlayerController Test', () {
    //   test("First Test", () {
    //     expect(player, isInstanceOf<PlayerController>());
    //   });

    //   test("Set Value", () {
    //     expect(player.value, equals(0));
    //     player.increment();
    //     expect(player.value, equals(1));
    //   });
  });
}
