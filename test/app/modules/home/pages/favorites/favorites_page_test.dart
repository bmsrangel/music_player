import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FavoritesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FavoritesPage(title: 'Favorites')));
    //  final titleFinder = find.text('Favorites');
    //  expect(titleFinder, findsOneWidget);
  });
}
