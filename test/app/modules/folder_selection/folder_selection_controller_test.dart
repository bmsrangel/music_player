import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:music_player/app/modules/folder_selection/folder_selection_module.dart';

void main() {
  initModule(FolderSelectionModule());
  // FolderSelectionController folderselection;
  //
  setUp(() {
    //     folderselection = FolderSelectionModule.to.get<FolderSelectionController>();
  });

  group('FolderSelectionController Test', () {
    //   test("First Test", () {
    //     expect(folderselection, isInstanceOf<FolderSelectionController>());
    //   });

    //   test("Set Value", () {
    //     expect(folderselection.value, equals(0));
    //     folderselection.increment();
    //     expect(folderselection.value, equals(1));
    //   });
  });
}
