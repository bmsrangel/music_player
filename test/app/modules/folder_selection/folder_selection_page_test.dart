import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FolderSelectionPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FolderSelectionPage(title: 'FolderSelection')));
    //  final titleFinder = find.text('FolderSelection');
    //  expect(titleFinder, findsOneWidget);
  });
}
